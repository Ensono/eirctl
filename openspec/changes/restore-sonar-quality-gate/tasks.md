## 1. Baseline and Safety Preconditions

- [x] 1.1 Load `SONAR_TOKEN` from `~/env/sonarcloud` into a subprocess environment without printing, persisting, or placing it in command arguments; query project `Ensono_eirctl` branch `main` and record the latest analysis key/revision/date, project version, previous-version period, quality-gate conditions, and statuses for every issue key named in this task list. **Postcondition:** the six Tier 0 keys are either confirmed open with their expected rules/components or implementation stops for scope review.
- [x] 1.2 Confirm the implementation base contains the analyzed `main` changes, the worktree has no unrelated edits in scoped files, all existing repository instructions apply, and the current focused package tests for `scripts/check-workflow-policy`, `scripts/materialize-sonar-source`, `internal/config`, `cmd/eirctl`, `output`, `variables`, `internal/cmdutils`, `internal/genci`, `internal/utils`, `runner`, and `scheduler` pass or have recorded pre-existing failures. **Postcondition:** no later task may hide or normalize a baseline failure.
- [x] 1.3 Refresh `openspec/changes/restore-sonar-quality-gate/evidence.md` with, for each scoped key, its rule, severity, assignee, component/line, pre-change message, gate tier, intended files, invariants, focused check, and stop condition. **Postcondition:** every issue task below can be completed or escalated independently by a smaller model and the ledger contains no credentials.

### Issue-task completion contract

Every issue checkbox in sections 2–8 is incomplete unless all five clauses are satisfied: **Precondition** — the refreshed key/rule/component and named code/test shape match; **Permitted change** — only the named bounded transformation and minimum directly related tests are modified; **Postcondition** — the named rule condition is structurally absent while stated behavior/assertions remain; **Checks** — every exact command named by the task passes and its outcome is recorded in `evidence.md`; **Stop/escalate** — drift, unrelated edits, missing characterization, excluded semantic/API/security work, or an unprovable postcondition leaves the checkbox open and records a defer/blocker instead of improvising. Task-specific wording below supplies additional invariants and checks.

## 2. Low-Risk Constants and Local Structural Fixes

- [x] 2.1 For `AZ-N-y0t3_y5QfimGTZ2`, confirm `scripts/check-workflow-policy/main.go` still duplicates the exact `validate-build` job identifier in expected-permission, lookup, and dependency checks; inspect the related topology tests, then introduce one private constant and replace only those literals. Preserve the YAML identifier, lookup behavior, check order, and error text. Run `gofmt`, `go test ./scripts/check-workflow-policy`, `go vet ./scripts/check-workflow-policy`, and `bash scripts/check-workflow-security.sh`; stop if any use has different semantics.
- [ ] 2.2 For `AZ-N-y0t3_y5QfimGTZ4`, confirm `scripts/check-workflow-policy/main.go` still repeats the `actions/github-script@` prefix and that immutable SHA validation remains a separate predicate; centralize only the prefix without converting prefix matching to exact matching. Run `gofmt`, `go test ./scripts/check-workflow-policy`, `go vet ./scripts/check-workflow-policy`, and `bash scripts/check-workflow-security.sh`; inspect the diff to confirm no trust predicate or action pin changed.
- [ ] 2.3 For `AZWKA6kX03lIpGBL3rEn`, confirm `cmd/eirctl/eirctl.go` repeats the `no-summary` Viper/flag key three times; introduce a package-local constant, preserve flag spelling/default/binding/lookup semantics, and run `gofmt` plus `go test ./cmd/eirctl` with the existing flag/config precedence cases.
- [ ] 2.4 For `AZWKA6j703lIpGBL3rEk`, confirm `cmd/eirctl/init.go` repeats the `no-prompt` key five times; introduce a local constant, preserve prompted/non-prompted behavior and binding errors, and run `gofmt` plus `go test ./cmd/eirctl`; stop if occurrences represent different external names.
- [ ] 2.5 For `AZZ8glSK1wqWcqTip8XF`, confirm `ContainerExecutor.WithReset` remains an intentional no-op required by the shared executor interface and container state is isolated per execution; add the nested explanatory comment required by `go:S1186` without changing interfaces or behavior, then run `gofmt` and `go test ./runner`.
- [ ] 2.6 For `AZxSHQDOhNFpWUf3zR1y`, confirm `runner/executor_default.go` creates `timeoutCancelFn` only when `job.Timeout` is non-nil and existing tests cover timeout and no-timeout execution; move the defer into the timeout branch or make the equivalently minimal structural correction without changing cancellation timing, then run `gofmt` and `go test ./runner`. Stop if the current code no longer guarantees cancellation on every timeout path.

## 3. Materializer Response Types

- [ ] 3.1 For `AZ-N-y1d3_y5QfimGTaC`, confirm the finding targets the anonymous pull-request base repository response beneath `pullResponse` in `scripts/materialize-sonar-source/main.go`; introduce a private named response type while preserving every JSON tag, field name, and zero-value behavior. Run `gofmt`, `go test ./scripts/materialize-sonar-source`, and `go vet ./scripts/materialize-sonar-source`; compare fixture decoding before and after.
- [ ] 3.2 For `AZ-N-y1d3_y5QfimGTaD`, confirm the finding targets the anonymous pull-request head repository response and reuse or add the smallest private named type consistent with task 3.1. Preserve the API response shape and all provenance, identity, path, blob, and head-recheck logic; run `gofmt`, `go test ./scripts/materialize-sonar-source`, `go vet ./scripts/materialize-sonar-source`, and `bash scripts/check-workflow-security.sh`.

## 4. Gate-Blocking Cache Tests

- [ ] 4.1 For `AZ-N-ywQ3_y5QfimGTZy`, confirm `Test_StoreInCache` in `internal/config/cache_test.go` still has complexity 23 and inventory its success, directory-creation, stream-copy, and mock-error assertions; extract fixture/setup or focused subtests without changing production cache code or deleting cases. Run `gofmt`, the focused test by name, and `go test ./internal/config`; compare case and assertion inventories before and after.
- [ ] 4.2 For `AZ-N-ywQ3_y5QfimGTZz`, confirm `Test_Get_fromCache` still has complexity 22 and inventory missing-file, unexpected-error, YAML/import, copy, and successful read assertions; split setup/execution/assertion helpers or focused tests while preserving all failure paths. Run `gofmt`, the focused test by name, and `go test ./internal/config`; stop if simplification would require changing cache ownership or close semantics.

## 5. Selected Assertion-Preserving Test Debt

- [ ] 5.1 For `AZ1t2WyVtvGp9fbwa0Kx`, confirm `cmdRunTestHelper` in `cmd/eirctl/eirctl_test.go` still combines context setup, command construction, execution/error handling, and output assertions; extract named helpers while preserving every caller, expected error, output assertion, and cancellation behavior. Run `gofmt` and `go test ./cmd/eirctl`; compare helper call coverage before and after.
- [ ] 5.2 For `AZ1t2Wy_tvGp9fbwa0K3`, confirm `Test_main` in `cmd/main_test.go` still has complexity 28 and inventory debug, verbose, unknown-command, help, and exit-code cases; isolate setup or table-drive cases without leaking `os.Args` or log state across subtests. Run `gofmt`, the focused test, and `go test ./cmd`; confirm exit codes `0`, `1`, and `125` remain asserted where applicable.
- [ ] 5.3 For `AZ1t2WvrtvGp9fbwa0Km`, confirm `TestOutput_prefixedOutputDecorator` still has complexity 20 and inventory header, transformed body, blank-line, trailing-newline, and footer assertions; extract lifecycle/assertion helpers without weakening byte-exact expectations. Run `gofmt`, the focused test, and `go test ./output`.
- [ ] 5.4 For `AZ1t2WzPtvGp9fbwa0K4`, confirm `TestVariables_MergeV2` still has complexity 19 and inventory merge, overwrite, chaining, key-preservation, and value assertions; extract an exact map/value assertion helper without changing `variables` production behavior. Run `gofmt`, the focused test, `go test ./variables`, and a race-enabled variables test if the existing suite exercises concurrency.
- [ ] 5.5 For `AZ1t2WjltvGp9fbwa0IF`, confirm `TestGenCi_GithubImpl_ordering` still has complexity 16 and protects nested graph, generated YAML, job, and step ordering; extract graph construction and repeated ordering assertions while preserving exact output-order checks. Run `gofmt`, the focused test, and `go test ./internal/genci`.
- [ ] 5.6 For `AZ1t2WqptvGp9fbwa0Ir`, confirm `TestMapKeys` still uses nested membership loops and does not reject unexpected keys; replace the loop with an exact sorted/set comparison that proves both missing and extra keys, retain non-map behavior, and run `gofmt`, the focused test, and `go test ./internal/utils`.
- [ ] 5.7 For `AZ1t2W1PtvGp9fbwa0LE`, confirm `TestExecutionGraph_Nodes` still combines graph construction, node lookup, missing-node error handling, and child assertions; extract focused helpers/subtests while preserving successful lookup, root-child membership, and `ErrNodeNotFound` assertions. Run `gofmt`, the focused test, `go test ./scheduler`, and `go test -race ./scheduler`.
- [ ] 5.8 For `AZ1t2W0ytvGp9fbwa0K8`, confirm `Test_Scheduler_Error_Required` is materially identical to `Test_Scheduler_ConditionErroredStage`; compare every fixture and assertion. If truly duplicate, retain the stronger condition-error test and replace the misleading duplicate with a genuine required-input test only if repository behavior defines one; otherwise remove only the duplicate. Run `gofmt`, both relevant focused tests, `go test ./scheduler`, and `go test -race ./scheduler`; stop if deletion would remove distinct coverage.

## 6. Selected Private Production Refactors

- [ ] 6.1 For `AZbPupl0Qf_52EOIHgtb`, confirm `internal/cmdutils.PrintSummary` still has complexity 17 and add characterization assertions for byte-exact output for each task status and error message before refactoring if they do not already exist; extract status-specific rendering while preserving sorting, stage order, labels, colors, and output text. Run `gofmt` and `go test ./internal/cmdutils`; stop if output compatibility cannot be proven.
- [ ] 6.2 For `AZWKA6je03lIpGBL3rEh`, confirm `runner.CompileTask` still has complexity 19 and inspect all compilation, variation, interactive-task, and empty-job tests; extract variable rendering and/or linked-list append helpers without changing command/variation order, template errors, timeout, environment, or interactive mutation. Run `gofmt`, focused compiler tests, and `go test ./runner`.
- [ ] 6.3 For `AZWKA6je03lIpGBL3rEi`, confirm private `compileCommand` still has ten positional parameters and all callers are package-local; introduce one private input struct grouping the existing values without changing value ownership or defaults, migrate every caller, and run `gofmt`, focused compiler/before/after/condition tests, and `go test ./runner`. Stop if a caller relies on positional omission or mutation not represented by the struct.

## 7. Git SSH Security-Sensitive Refactor

- [ ] 7.1 For `AZ-N-yxh3_y5QfimGTZ0`, confirm `internal/config.processSSHConfig` remains the sole merge step called by `getGitSSHAuth`, has complexity 19, returns no novel errors, and implements explicit-command-over-file precedence. Add or strengthen characterization tests for file-provided scalar values, defaults (`22`, `git`, requested host), explicit scalar/identity precedence, repeated quoted user/system known-host entries, legacy first-entry fields, and explicit strict-host-key precedence before refactoring.
- [ ] 7.2 Resolve `AZ-N-yxh3_y5QfimGTZ0` by extracting focused scalar-default, identity, known-host, and strict-host-key helpers while preserving the existing function signature, call order, lookup-error behavior, defaults, repeated entries, explicit insecure opt-out warning, and fail-closed trust handling. Run `gofmt`, `go test ./internal/config`, and `go test -race ./internal/config`; inspect the diff against the characterization inventory and stop if broader SSH parsing or error-contract changes become necessary.
- [ ] 7.3 Obtain review from a reviewer who did not implement the Git SSH slice and record the PR review URL or reviewer identity/checklist in `evidence.md`; explicitly check unknown/changed/hashed host keys, configured/default trust files, alias/port handling, explicit command precedence, repeated known-host paths, and `StrictHostKeyChecking=no`. Link every concern to its resolution or explicit disposition and rerun `go test ./internal/config` plus `go test -race ./internal/config`; keep the task incomplete until approval and rerun evidence are recorded.

## 8. Workflow-Policy Security-Sensitive Refactor

- [ ] 8.1 For `AZ-N-y0t3_y5QfimGTZ7`, confirm `validateRepositoryTopology` in `scripts/check-workflow-policy/main.go` still has complexity 26 and record in `evidence.md` a line-by-line predicate/order inventory covering protected policy, broker, builder, publisher, release, Scorecard, and trusted Sonar topology checks plus their exact errors and mutation tests.
- [ ] 8.2 Resolve `AZ-N-y0t3_y5QfimGTZ7` by extracting explicit ordered validation phases for the existing trust domains; preserve fail-fast order, short-circuiting, exact predicates, action-prefix matching, immutable-pin validation, permission checks, checkout restrictions, static-main verification, provenance checks, and error semantics. **Postcondition:** the orchestration function is below the configured complexity limit and the predicate inventory is unchanged. Do not introduce a generic policy engine or change workflow behavior. Run `gofmt`, `go test ./scripts/check-workflow-policy`, `go vet ./scripts/check-workflow-policy`, `bash scripts/check-workflow-security.sh`, `bash scripts/check-immutable-ci-dependencies.sh`, and `bash scripts/check-codeowners.sh`; stop if any predicate cannot be mapped one-to-one.
- [ ] 8.3 Obtain review from a reviewer who did not implement the workflow-policy slice and record the PR review URL or reviewer identity/checklist in `evidence.md`; use the pre-change predicate inventory and hostile mutation matrix to confirm no validation was omitted, reordered, broadened, or made candidate-controlled. Link every concern to its resolution or explicit disposition and rerun every check from task 8.2; keep the task incomplete until approval and rerun evidence are recorded.

## 9. Repository-Wide Verification

- [ ] 9.1 Run `gofmt` verification for all changed Go files; `go test ./...`; `go test -race ./internal/config ./runner ./scheduler`; `bash scripts/check-workflow-security.sh`; `bash scripts/check-immutable-ci-dependencies.sh`; `bash scripts/check-codeowners.sh`; schema generation followed by `git diff --exit-code -- schemas`; and `go run cmd/main.go run pipeline gha:unit:test --verbose`. Record exact outcomes in `evidence.md`; if an environment-dependent command cannot run, record the exact blocker and keep acceptance incomplete until equivalent protected CI evidence exists. Record rather than hide any known unrelated full-`go vet` baseline finding.
- [ ] 9.2 Inspect the complete implementation diff and prove that it contains no dependency additions, public API changes, Sonar/workflow configuration changes, coverage/report-path changes, issue suppressions, exclusions, generated-file drift, weakened assertions, or unrelated refactors. Confirm each scoped key maps to all required completed preparation/implementation/review tasks and one final disposition in `evidence.md`, and every issue outside the scoped ledger remains untouched.
- [ ] 9.3 Run protected pull-request Sonar analysis for the exact implementation revision and confirm the stable `SonarCloud Code Analysis` check passes with expected coverage, no new scoped Critical/Major issue, zero credential exposure, and unchanged project/scanner/report configuration. Treat scanner/API status as authoritative rather than local structural assumptions.
- [ ] 9.4 After protected merge, query the exact trusted-main analysis and all scoped issue keys using the token-safe method from task 1.1; require every Tier 0 and Tier 1 key to be closed or absent, and require every Tier 2 key to be closed/absent or to have an authenticated drift/defer disposition that did not delay Tier 0/Tier 1. Confirm no replacement gate-relevant Critical smell exists, the unchanged `new_code_smells_severity` measure is below its configured threshold for the unchanged new-code period, coverage and every other condition pass, and overall gate status is green. If any condition fails, stop and record evidence without changing the gate, period, issue status, exclusions, or branch rules.
- [ ] 9.5 Record final analysis/run URLs, exact revision, gate conditions, per-key dispositions, commands, independent review records, residual deferred debt, and rollback commits in `openspec/changes/restore-sonar-quality-gate/evidence.md`; do not archive the change until the green trusted-main evidence and all mandatory task checkboxes are complete.
