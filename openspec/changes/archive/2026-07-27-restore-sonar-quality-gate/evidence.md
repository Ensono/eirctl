# SonarCloud Evidence Ledger

This token-free planning snapshot was obtained through authenticated SonarCloud API calls on 2026-07-27. `SONAR_TOKEN` was loaded only into the querying subprocess and was not printed, persisted, or placed in command arguments. Implementation task 1.1 must refresh this ledger before editing because issue status and locations can drift.

## Refreshed pre-change evidence (2026-07-27)

Task 1.1 re-queried `Ensono_eirctl` / `main` using a subprocess-only `SONAR_TOKEN` and an in-process HTTP Authorization header (the token was neither printed, persisted, nor placed in command arguments). The latest analysis remains `67a4b92e-0472-47fa-98d9-b2bfc6e2cc72` for revision `bb3d10dc2360a50752e2c73fe507c427bf14af92` at `2026-07-27T08:38:23+0000`, version `0.11.4`. Its previous-version period begins at `0.11.3` / `2026-05-27T15:20:06+0000`; the gate remains `ERROR` solely because `new_code_smells_severity` is `20` against the unchanged `GT 14` threshold. The other conditions remain `OK`: new coverage `82.8` (`LT 80`), duplicated-lines density `0.0` (`GT 3`), bugs severity `0` (`GT 9`), and vulnerabilities severity `0` (`GT 9`).

All 23 scoped keys were returned by the authenticated query. Every Tier 0 key remains `OPEN` with the rule, severity, component, and line recorded in the table below; no mandatory key drifted or was missing. The current checkout contains the analyzed revision (`bb3d10dc2360a50752e2c73fe507c427bf14af92` is an ancestor of `HEAD`).

## Baseline checks (2026-07-27)

The worktree contained only the OpenSpec change directory before implementation; no scoped source or test file had unrelated edits, and `git diff --check` passed. The repository-level `AGENTS.md` instructions apply. The following focused baseline completed successfully:

```text
go test ./scripts/check-workflow-policy ./scripts/materialize-sonar-source ./internal/config ./cmd/eirctl ./output ./variables ./internal/cmdutils ./internal/genci ./internal/utils ./runner ./scheduler
```

No focused-package baseline failures were observed; later tasks must not normalize any failure as pre-existing.

## Analysis and gate baseline

- Project/branch: `Ensono_eirctl` / `main`
- Analysis key: `67a4b92e-0472-47fa-98d9-b2bfc6e2cc72`
- Analysis date: `2026-07-27T08:38:23+0000`
- Revision: `bb3d10dc2360a50752e2c73fe507c427bf14af92`
- Project version: `0.11.4`
- Previous-version period: begins at version `0.11.3` on `2026-05-27T15:20:06Z`, as recorded by the archived authenticated verification at `openspec/changes/archive/2026-07-27-secure-sonarcloud-pr-analysis/verification.md`
- Overall gate: `ERROR`

| Condition | Status | Actual | Comparator / threshold |
|---|---|---:|---|
| `new_coverage` | OK | 82.8 | `LT 80` |
| `new_duplicated_lines_density` | OK | 0.0 | `GT 3` |
| `new_bugs_severity` | OK | 0 | `GT 9` |
| `new_code_smells_severity` | ERROR | 20 | `GT 14` |
| `new_vulnerabilities_severity` | OK | 0 | `GT 9` |

## Scoped issue snapshot

“Gate blocker” is based on the archived authenticated previous-version attribution, updated for the main analysis that automatically closed `AZ-N-y0t3_y5QfimGTZ3` as fixed. Final acceptance still requires a fresh measure/issue query because the aggregate gate is authoritative.

| Tier | Gate blocker | Issue | Status | Rule | Severity | Assignee | Component |
|---:|:---:|---|---|---|---|---|---|
| 0 | Yes | `AZ-N-ywQ3_y5QfimGTZy` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `internal/config/cache_test.go:53` |
| 0 | Yes | `AZ-N-ywQ3_y5QfimGTZz` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `internal/config/cache_test.go:126` |
| 0 | Yes | `AZ-N-yxh3_y5QfimGTZ0` | OPEN | `go:S3776` | CRITICAL | `richards-ensono-Gqhdf@github` | `internal/config/loader_git.go:559` |
| 0 | Yes | `AZ-N-y0t3_y5QfimGTZ2` | OPEN | `go:S1192` | CRITICAL | `richards-ensono-Gqhdf@github` | `scripts/check-workflow-policy/main.go:344` |
| 0 | Yes | `AZ-N-y0t3_y5QfimGTZ7` | OPEN | `go:S3776` | CRITICAL | `richards-ensono-Gqhdf@github` | `scripts/check-workflow-policy/main.go:365` |
| 0 | Yes | `AZ-N-y0t3_y5QfimGTZ4` | OPEN | `go:S1192` | CRITICAL | `richards-ensono-Gqhdf@github` | `scripts/check-workflow-policy/main.go:387` |
| 1 | No | `AZ-N-y1d3_y5QfimGTaC` | OPEN | `godre:S8205` | MINOR | `richards-ensono-Gqhdf@github` | `scripts/materialize-sonar-source/main.go:96` |
| 1 | No | `AZ-N-y1d3_y5QfimGTaD` | OPEN | `godre:S8205` | MINOR | `richards-ensono-Gqhdf@github` | `scripts/materialize-sonar-source/main.go:102` |
| 2 | No | `AZWKA6kX03lIpGBL3rEn` | OPEN | `go:S1192` | CRITICAL | `dnitsch@github` | `cmd/eirctl/eirctl.go:74` |
| 2 | No | `AZWKA6j703lIpGBL3rEk` | OPEN | `go:S1192` | CRITICAL | `dnitsch@github` | `cmd/eirctl/init.go:59` |
| 2 | No | `AZ1t2WyVtvGp9fbwa0Kx` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `cmd/eirctl/eirctl_test.go:40` |
| 2 | No | `AZ1t2Wy_tvGp9fbwa0K3` | OPEN | `go:S3776` | CRITICAL | unassigned | `cmd/main_test.go:13` |
| 2 | No | `AZ1t2WvrtvGp9fbwa0Km` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `output/prefixed_test.go:12` |
| 2 | No | `AZ1t2WzPtvGp9fbwa0K4` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `variables/variables_test.go:51` |
| 2 | No | `AZbPupl0Qf_52EOIHgtb` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `internal/cmdutils/cmdutils.go:63` |
| 2 | No | `AZ1t2WjltvGp9fbwa0IF` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `internal/genci/githubimpl_test.go:58` |
| 2 | No | `AZ1t2WqptvGp9fbwa0Ir` | OPEN | `go:S3776` | CRITICAL | unassigned | `internal/utils/utils_test.go:152` |
| 2 | No | `AZWKA6je03lIpGBL3rEh` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `runner/compiler.go:27` |
| 2 | No | `AZWKA6je03lIpGBL3rEi` | OPEN | `go:S107` | MAJOR | `dnitsch@github` | `runner/compiler.go:84` |
| 2 | No | `AZZ8glSK1wqWcqTip8XF` | OPEN | `go:S1186` | CRITICAL | `dnitsch@github` | `runner/executor_container.go:102` |
| 2 | No | `AZxSHQDOhNFpWUf3zR1y` | OPEN | `godre:S8188` | MAJOR | `dnitsch@github` | `runner/executor_default.go:83` |
| 2 | No | `AZ1t2W0ytvGp9fbwa0K8` | OPEN | `go:S4144` | MAJOR | `dnitsch@github` | `scheduler/scheduler_test.go:263` |
| 2 | No | `AZ1t2W1PtvGp9fbwa0LE` | OPEN | `go:S3776` | CRITICAL | `dnitsch@github` | `scheduler/graph_test.go:60` |

## Tier 2 admission rule

Tier 2 addresses the user's request to include other safe Critical/Major findings, but it must not delay or broaden Tier 0/Tier 1 recovery. A Tier 2 issue is admitted only if refreshed evidence still matches this ledger, focused tests characterize the behavior, the change remains private and behavior-preserving, and the task-specific postconditions can be proven. Otherwise it receives an authenticated drift/defer disposition and moves to a separate proposal.

## Implementation records

During `/opsx-apply`, update this file with:

- refreshed pre-change API evidence;
- per-key precondition and final disposition;
- links or commit IDs for implementation slices;
- independent review records for Git SSH and workflow policy;
- focused and repository-wide command results;
- protected PR analysis/run URLs;
- exact accepted pull-request revision and gate conditions;
- residual deferred findings and rollback commits.

No credentials, private token metadata, or secret values belong in this ledger.

## Per-key execution ledger

The authenticated snapshot above is the precondition for every row. “Stop” always includes an issue that is closed/moved, a mismatched rule/component, unrelated scoped-file edits, missing characterization, or a required behavior/API/security change. Each focused check must pass and be recorded before its task can be checked.

| Key | Tier | Intended files and bounded invariant | Focused check | Stop condition |
|---|---:|---|---|---|
| `AZ-N-ywQ3_y5QfimGTZy` | 0 | `internal/config/cache_test.go`; split only setup/assertion complexity and retain success, mkdir, copy, and mock-error cases. | `go test ./internal/config -run Test_StoreInCache`; `go test ./internal/config` | Production cache behavior or any assertion would change. |
| `AZ-N-ywQ3_y5QfimGTZz` | 0 | `internal/config/cache_test.go`; retain missing-file, error, YAML/import, copy, and successful-read paths. | `go test ./internal/config -run Test_Get_fromCache`; `go test ./internal/config` | Cache ownership or close semantics must change. |
| `AZ-N-yxh3_y5QfimGTZ0` | 0 | `internal/config/loader_git.go` and tests; preserve command precedence, defaults, repeated trust paths, explicit opt-out warning, and fail-closed trust. | `go test ./internal/config`; `go test -race ./internal/config` | SSH parsing or error contract would broaden/change. |
| `AZ-N-y0t3_y5QfimGTZ2` | 0 | `scripts/check-workflow-policy/main.go`; one private `validate-build` identifier constant only. | `go test ./scripts/check-workflow-policy`; `go vet ./scripts/check-workflow-policy`; `bash scripts/check-workflow-security.sh` | Any literal has different semantics. |
| `AZ-N-y0t3_y5QfimGTZ7` | 0 | `scripts/check-workflow-policy/main.go`; explicit ordered topology phases with one-to-one predicates/errors. | policy tests, vet, workflow-security, immutable-dependencies, and CODEOWNERS checks | Any predicate/order/trust boundary cannot be mapped. |
| `AZ-N-y0t3_y5QfimGTZ4` | 0 | `scripts/check-workflow-policy/main.go`; centralize only `actions/github-script@`; retain prefix and independent SHA validation. | policy tests, vet, and workflow-security check | Prefix becomes exact matching or any pin/predicate changes. |
| `AZ-N-y1d3_y5QfimGTaC` | 1 | `scripts/materialize-sonar-source/main.go`; private named base-repository response preserves all JSON names/tags/zeros. | `go test ./scripts/materialize-sonar-source`; `go vet ./scripts/materialize-sonar-source` | Fixture decoding changes. |
| `AZ-N-y1d3_y5QfimGTaD` | 1 | Same file; smallest named head-repository response type preserves provenance/path/blob/head-recheck behavior. | materializer tests, vet, workflow-security check | API response shape or provenance logic changes. |
| `AZWKA6kX03lIpGBL3rEn` | 2 | `cmd/eirctl/eirctl.go`; private `no-summary` key constant preserves flag/default/binding/lookup. | `go test ./cmd/eirctl` | Flag/config precedence changes. |
| `AZWKA6j703lIpGBL3rEk` | 2 | `cmd/eirctl/init.go`; private `no-prompt` key constant preserves prompt behavior/binding errors. | `go test ./cmd/eirctl` | Occurrences are distinct external names. |
| `AZZ8glSK1wqWcqTip8XF` | 2 | `runner/executor_container.go`; explanatory no-op comment only. | `go test ./runner` | Interface or execution isolation would change. |
| `AZxSHQDOhNFpWUf3zR1y` | 2 | `runner/executor_default.go`; defer timeout cancel in its creation branch with unchanged timing. | `go test ./runner` | A timeout path cannot guarantee cancellation. |
| `AZ1t2WyVtvGp9fbwa0Kx` | 2 | `cmd/eirctl/eirctl_test.go`; named helpers retain callers, errors, output, and cancellation. | `go test ./cmd/eirctl` | Helper coverage/assertions weaken. |
| `AZ1t2Wy_tvGp9fbwa0K3` | 2 | `cmd/main_test.go`; isolate cases without leaking args/log state; retain 0/1/125 assertions. | `go test ./cmd -run Test_main`; `go test ./cmd` | Case inventory changes. |
| `AZ1t2WvrtvGp9fbwa0Km` | 2 | `output/prefixed_test.go`; helpers retain byte-exact header/body/blank/newline/footer checks. | `go test ./output -run TestOutput_prefixedOutputDecorator`; `go test ./output` | Exact output assertions weaken. |
| `AZ1t2WzPtvGp9fbwa0K4` | 2 | `variables/variables_test.go`; exact map/value assertion helper only. | `go test ./variables -run TestVariables_MergeV2`; `go test ./variables` | Production merge behavior changes. |
| `AZ1t2WjltvGp9fbwa0IF` | 2 | `internal/genci/githubimpl_test.go`; preserve graph/YAML/job/step ordering. | `go test ./internal/genci -run TestGenCi_GithubImpl_ordering`; `go test ./internal/genci` | Any exact order check weakens. |
| `AZ1t2WqptvGp9fbwa0Ir` | 2 | `internal/utils/utils_test.go`; exact key set must reject extras and retain non-map case. | `go test ./internal/utils -run TestMapKeys`; `go test ./internal/utils` | Non-map behavior changes. |
| `AZ1t2W1PtvGp9fbwa0LE` | 2 | `scheduler/graph_test.go`; preserve lookup, root-child, and `ErrNodeNotFound`. | focused test; `go test ./scheduler`; `go test -race ./scheduler` | Any graph assertion is lost. |
| `AZ1t2W0ytvGp9fbwa0K8` | 2 | `scheduler/scheduler_test.go`; remove only a proven duplicate or add genuine required-input coverage. | both focused tests; scheduler and race tests | Coverage is distinct. |
| `AZbPupl0Qf_52EOIHgtb` | 2 | `internal/cmdutils`; characterize byte-exact statuses/errors before status renderer extraction. | `go test ./internal/cmdutils` | Output compatibility cannot be proven. |
| `AZWKA6je03lIpGBL3rEh` | 2 | `runner/compiler.go`; helpers preserve ordering, templates, timeout/environment, interactive mutation. | focused compiler tests; `go test ./runner` | Any compile contract changes. |
| `AZWKA6je03lIpGBL3rEi` | 2 | `runner/compiler.go`; private input struct replaces ten arguments without ownership/default changes. | focused compiler tests; `go test ./runner` | Caller needs positional omission/mutation not represented by struct. |

Independent review is mandatory before marking the Git SSH and workflow-policy review tasks complete. Final issue closure and gate acceptance for this change are contingent on authenticated protected PR analysis; trusted-main monitoring is retained in protected branch governance.

## Workflow-policy topology predicate inventory (task 8.1)

Pre-change orchestration at `scripts/check-workflow-policy/main.go:367-445` is fail-fast in the following exact order. Every `requiredWorkflow` call first returns `required workflow <path> is missing` when absent.

1. **Protected policy boundary** (`trusted-workflow-policy.yml`): require job `policy`; trigger `pull_request_target`; at least three steps; step 0 is the approved checkout with implicit protected-base ref, `fetch-depth: 1`, and `persist-credentials: false`; step 2 contains both `scripts/materialize-policy-candidate` and `go run ./scripts/check-workflow-policy --candidate-root`. Combined error: `trusted workflow policy must check out only the implicit protected base revision and inspect candidate configuration as data`.
2. **Debug request broker** (`debug-build-request.yml`), preserving two sequential error groups:
   - require job `request`, trigger `issue_comment`, concurrency `debug-build-request-${{ github.event.issue.number }}`, exact `/build-debug` condition, and no checkout. Error: `debug build broker must authorize exact requests, serialize them per PR, and never check out code`.
   - require only `actions: write` and `pull-requests: read`; a `actions/github-script@` use; script fragments `createWorkflowDispatch`, `workflow_id: 'debug-build.yml'`, and `ref: 'main'`. Error: `debug build broker must have only pull-request read and workflow-dispatch authority`.
3. **Untrusted debug builder** (`debug-build.yml`): require job `build`, trigger `workflow_dispatch`, checkout ref `${{ inputs.commit_sha }}`, GitHub-script pull lookup containing `github.rest.pulls.get` and `pullRequest.head.sha`, no environment, and no secret reference. Error: `debug build must validate dispatched pull-request identity before an immutable read-only checkout without environment or secrets`.
4. **Trusted debug publisher** (`publish-debug-release.yml`): require trigger `workflow_dispatch`, jobs `validate-build` and `publish`, both guarded by `github.ref == 'refs/heads/main'`; validate permissions `actions: read, contents: read` with no environment; publish permissions `actions: read, contents: write`, environment `debug-release`, and need `validate-build`; validation script contains `run.event !== 'workflow_dispatch'`; neither job checks out. Error: `debug publication must validate read-only data before its isolated debug-release contents-write job`.
5. **Release workflows**, in file order `release.yml` then `release_container.yml`, and each parsed job's existing map iteration order: require the complete trusted successful-push guard and a static `main` checkout with `persist-credentials: false`, followed by exact `Verify the validated workflow-run revision` environment/run verification. Error: `<file> job <job-id> must require a successful trusted push and verify a static protected-main checkout against its workflow-run SHA`.
6. **Scorecard** (`scorecard.yml`): require job `analysis` and checkout with `persist-credentials: false`. Error: `scorecard must use job-scoped permissions and a checkout without credentials`.
7. **Trusted Sonar topology**, delegated last to `validateTrustedSonarCloudTopology` and preserving its internal fail-fast order:
   - required `trusted-sonarcloud-pr.yml`;
   - envelope: one job, `workflow_run` for only `Lint and Test`, workflow `contents: read` — `trusted SonarCloud analyzer must use only the expected read-only workflow_run topology`;
   - job: `analyze`, no environment/job permissions/container/services, successful PR and same-repository guards, exact stale-run concurrency — `trusted SonarCloud analyzer must require a successful PR run, no container or services, and stale-revision-cancelling same-repository or fork concurrency`;
   - no cache/local action/workflow or job secret — `trusted SonarCloud analyzer must not use caches, local actions, or workflow/job-scoped secrets`;
   - exactly seven ordered steps — `trusted SonarCloud analyzer must use only the seven reviewed helper, provenance, report, materializer, and scanner steps`;
   - protected sparse helper checkout with one checkout only — `trusted SonarCloud analyzer may check out only the protected main helper and report validator`;
   - immutable workflow/run/attempt/PR/base/head/revision/artifact provenance predicates — `trusted SonarCloud analyzer must bind workflow, run, attempt, PR, base/head repositories, revision, and report artifact provenance`;
   - exact verified artifact download and passive report validation — `trusted SonarCloud analyzer must download and validate only the exact verified passive report artifact`;
   - exact forced scanner configuration — `trusted SonarCloud analyzer must create only the exact forced scanner configuration outside the passive source root`;
   - exact bounded Git Data API materializer command and sole `GH_TOKEN` environment — `trusted SonarCloud analyzer must use only the protected bounded Git Data API materializer with the verified head repository and SHA`;
   - exact immutable scanner action/version/binary URL/signature setting/arguments — `trusted SonarCloud analyzer must end with only the approved immutable scanner, runtime, endpoint, project, report, PR, revision, and quality-gate settings`;
   - only scanner step index 6 receives `SONAR_TOKEN` — `trusted SonarCloud analyzer must scope SONAR_TOKEN to the approved scanner step`.

### Existing hostile mutation and regression mapping

- Baseline acceptance: `TestWorkflowTopologyAndPermissions`.
- Protected policy boundary: `TestCandidateCannotReplaceTrustedPolicyBoundary` injects a candidate-controlled dynamic checkout and requires rejection; `TestMaterializerArchivesOnlyConfigurationData` ensures candidate data cannot replace checker code. Earlier `rejectScorecardDangerousCheckouts` may reject the dynamic checkout before topology, which is part of the existing fail-fast behavior.
- Broker/builder/publisher: `TestPrivilegedPRExecutionFixtures` and `TestStructuralPrivilegedFlowAnalysisRejectsDynamicExecution` cover privileged dynamic execution; `TestPolicyRejectsLegacyLabelBroker` rejects altered broker authority during per-workflow validation; `TestPolicyRejectsUnguardedDebugPublisher` reaches and rejects the publisher guard topology.
- Release: `TestTrustedWorkflowRunRequiresAllGuards` removes each trusted-push guard; `TestReleaseWorkflowsRequireVerifiedStaticMainCheckout` mutates dynamic refs in both release workflows, removes immutable revision verification, and restores checkout credentials.
- Scorecard: the candidate dynamic-checkout mutation above requires the exact `Scorecard Dangerous-Workflow` rejection; baseline acceptance covers the repository Scorecard job/checkout predicate.
- Trusted Sonar: `TestTrustedSonarCloudAnalyzerPolicy` preserves valid same-repository/fork and equivalent-trigger cases and rejects hostile mutations for fork association/concurrency/query ambiguity, PR state/base/head/revision, checkout aliases, mutable/derived source refs, materializer identity/bounds/path/blob/tree behavior, archive extraction, artifact identity/download, provenance, workflow/job secret scope, endpoint/report/config duplication, mutable/alternate/unsigned scanner, post-materialization execution, cache, container/service, and alternate scanner action. The test compares acceptance/rejection rather than exact error strings, so task 8.2 must preserve both the ordered helper calls and every exact error above.

Task 8.1 inventory check: `go test ./scripts/check-workflow-policy` passed on 2026-07-27. No predicate, test, or workflow file was changed while recording this inventory.

## Implementation results

- Archive scope disposition (2026-07-27): the user explicitly removed post-merge trusted-main verification and final branch-governance recordkeeping from this change's tasks. The archived change accepts the exact protected PR revision and its authoritative Sonar result; ordinary protected-branch monitoring remains outside the archived plan and is represented by PR #133 and branch governance rather than retained OpenSpec tasks.
- Task 9.3 corrected protected PR analysis (2026-07-27): PR [#133](https://github.com/Ensono/eirctl/pull/133) passed protected `Lint and Test` run [30262224929](https://github.com/Ensono/eirctl/actions/runs/30262224929) and trusted analyzer run [30262560164](https://github.com/Ensono/eirctl/actions/runs/30262560164) for exact source revision `70eed05bf73eb38ff88bf7a9c2883095046836b8`. The stable `SonarCloud Code Analysis` check passed. Authenticated token-safe API results were gate `OK`, coverage `86.5` (threshold 80), duplicated-lines density `0.0` (threshold 3), bugs severity `0`, vulnerabilities severity `0`, code-smell severity `0` (threshold 14), and zero open PR issues. Both replacement keys were absent, no new scoped Critical/Major issue existed, and the scanner/project/report/credential topology remained unchanged with zero credential exposure. This is authoritative PR acceptance, not trusted-main closure.
- Task 9.3 first protected PR analysis (2026-07-27, acceptance incomplete): PR [#133](https://github.com/Ensono/eirctl/pull/133) analyzed exact revision `48629f44a63e085d0488e6c8a7d5e8aed243f630`. Protected `Lint and Test` run [30261412176](https://github.com/Ensono/eirctl/actions/runs/30261412176) passed lint, schema validation, the containerized `gha:unit:test` pipeline, report generation, and bounded report upload, supplying the equivalent protected evidence required by task 9.1 after the local Docker blocker. Trusted analyzer run [30261738498](https://github.com/Ensono/eirctl/actions/runs/30261738498) preserved the reviewed provenance/report/materializer/token boundary but failed the authoritative PR gate. Authenticated token-safe API results: coverage `86.6` (OK, threshold 80), duplication `0.0` (OK), bugs `0` (OK), vulnerabilities `0` (OK), and `new_code_smells_severity=20` (ERROR, threshold 14) from exactly two new Critical `go:S3776` issues: `AZ-jUiJuZF5_tklsHNSW` at `internal/config/loader_git.go:623` (complexity 21) and `AZ-jUiMHZF5_tklsHNSX` at `internal/config/loader_git_hostkey_test.go:328` (complexity 28). No credential exposure or Sonar/scanner/report configuration drift was observed. Tasks 7.5–7.7 were added for bounded remediation and renewed review; task 9.3 remains incomplete and no gate, period, issue status, exclusion, or branch rule was changed.
- Task 9.2 complete-diff review (2026-07-27): parent inspection and an independent fresh-context builtin `reviewer` reviewed analyzed base `bb3d10dc2360a50752e2c73fe507c427bf14af92` through `b705c34` and marked the scope **APPROVED** with no blockers. Commit `20c3be2` was identified as the expected prior OpenSpec archive; implementation begins afterward. No dependency manifests, live `.github`/Sonar configuration, coverage/report paths, schemas, or generated files changed; no exported signature/public type changed; no `NOSONAR`, `nolint`, issue acceptance/reclassification, exclusion, threshold, or gate bypass was added. Every implementation/test path maps to a task, assertions were preserved or strengthened, and the one removed scheduler case is the recorded exact duplicate. All 23 scoped keys have preparation and local disposition records; Git SSH and workflow-policy keys also have required independent approvals. The reviewer ran commit-range diff checks, schema checks, `go test ./...`, and race tests; all passed. This scope approval does not claim authenticated Sonar closure.
- Task 9.1 repository verification (2026-07-27, acceptance incomplete): `gofmt -d` was clean for all 21 Go files changed since analyzed revision `bb3d10dc2360a50752e2c73fe507c427bf14af92`; `go test ./...`; `go test -race ./internal/config ./runner ./scheduler`; `bash scripts/check-workflow-security.sh`; `bash scripts/check-immutable-ci-dependencies.sh`; `bash scripts/check-codeowners.sh`; `go run tools/schemagenerator/main.go`; and `git diff --exit-code -- schemas` all passed. `go run cmd/main.go run pipeline gha:unit:test --verbose` failed before tests because the local Docker daemon was unavailable at `unix:///var/run/docker.sock`; the failing stage was `test:unit->test_prereqs`, and `sonar:coverage:prep` was cancelled. This environment blocker was recorded rather than bypassed; protected PR run `30261412176` subsequently ran the same containerized pipeline successfully, completing task 9.1 with equivalent protected CI evidence. A diagnostic `go vet ./...` retained the known unrelated baseline finding at `runner/executor_container_test.go:393` (`container.InspectResponse` unkeyed fields); focused policy vet passed and this baseline was not hidden or changed.

- `AZ-N-yxh3_y5QfimGTZ0` (Tier 0, task 7.1): verified `getGitSSHAuth` calls `processSSHConfig` exactly once as its sole SSH-file merge step. The existing merge function has no error-producing operation and preserves explicit command values by filling only empty fields. Added characterization coverage for file-derived port/user/hostname/identity/strict-host-key values, defaults (`22`, `git`, requested hostname), repeated quoted user/system known-host directives and their legacy first-entry fields, plus command scalar, identity, known-host, and `StrictHostKeyChecking=no` precedence. `gofmt`, `go test ./internal/config`, and `go test -race ./internal/config` passed on 2026-07-27. The subsequent extraction must preserve this inventory; local validation is not a claim of Sonar closure.
- `AZ-N-yxh3_y5QfimGTZ0` (Tier 0, task 7.2): extracted scalar-default, identity, known-host, and strict-host-key merge helpers beneath the unchanged `processSSHConfig` signature. The orchestration retains the original port/user/hostname, identity, user/system known-host, and strict-host-key order; all lookups still ignore the same library errors and fill only empty command-derived fields. Defaults, repeated entries, legacy first paths, the explicit `StrictHostKeyChecking=no` warning, and fail-closed host-key callback behavior are unchanged. `gofmt`, `go test ./internal/config`, and `go test -race ./internal/config` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ-N-yxh3_y5QfimGTZ0` (Tier 0, first independent review): an independent fresh-context builtin `reviewer` reviewed commits `8105b5c` and `a6e1c97` and reran `go test ./internal/config`, `go test -race ./internal/config`, `go test ./...`, and `git diff --check 8105b5c^..a6e1c97` successfully. The reviewer approved the existing coverage for unknown/changed/hashed keys, configured/default trust files, alias/port handling, command scalar precedence, repeated user known-host paths, and explicit `StrictHostKeyChecking=no`. It identified two High blockers: (1) `parseGitSshCommandEnv` recognized `SystemKnownHostsFile`, but OpenSSH uses `GlobalKnownHostsFile`, so a command-supplied global trust file was ignored; (2) file-supplied escaped or multiple known-host paths were not tokenized before validation. The change artifacts were revised and validated in commit `92842ce` to make both corrections mandatory; the first review is not an approval.
- `AZ-N-yxh3_y5QfimGTZ0` (Tier 0, task 7.3): added failing-first regression coverage for repeated quoted `GlobalKnownHostsFile` values in both supported `GIT_SSH_COMMAND -o` forms, command-over-file precedence, quoted and escaped spaces, multiple paths on one SSH-file directive, repeated user/global directives, Windows drive paths, UNC paths, order, and legacy first-entry fields. The parser now recognizes standard `GlobalKnownHostsFile` while retaining the existing `SystemKnownHostsFile` compatibility spelling, collects both in command order, and reads raw direct SSH-config directive text through the dependency's public node API before field splitting so quote/escape/path boundaries are not lost. Platform backslashes are protected before the existing shell parser processes quoting and escaped spaces. Public APIs, user-known-host behavior, alias/port/default handling, opt-out behavior, configured-file validation, and fail-closed host-key verification are unchanged. `gofmt`, `go test ./internal/config -run 'TestGitSSHCommandPreservesGlobalKnownHostsOptions|TestProcessSSHConfigMergesFileValuesAndPreservesCommandPrecedence'`, `go test ./internal/config`, and `go test -race ./internal/config` passed on 2026-07-27. Local validation is not a claim of Sonar closure.
- `AZ-N-yxh3_y5QfimGTZ0` (Tier 0, second independent review and task 7.3 follow-up): an independent fresh-context builtin `reviewer` reviewed through `dbb9563`; unknown/changed/hashed keys, defaults, direct file parsing, command precedence, opt-out behavior, fail-closed validation, public API stability, and complexity structure passed review. The reviewer reran `go test ./internal/config`, `go test -race ./internal/config`, `go test ./...`, and `git diff --check 8105b5c^..dbb9563` successfully but found one High blocker: included SSH configurations returned dependency values without the direct-node raw-boundary parser. The follow-up now parses included escaped and mixed/repeated quoted values, and safely disambiguates dependency-stripped single quoted paths from unquoted path lists using the required trust files' existence; if both interpretations exist it returns an explicit fail-closed ambiguity error. Added included-config coverage for quoted and escaped spaces, multiple paths, mixed quoted/unquoted paths, repeated user/global directives, ordering, and legacy first entries. `gofmt`, the focused Git SSH regression tests, `go test ./internal/config`, and `go test -race ./internal/config` passed on 2026-07-27.
- `AZ-N-yxh3_y5QfimGTZ0` (Tier 0, third independent review and task 7.3 follow-up): an independent fresh-context builtin `reviewer` reviewed through `b697220` and reran `go test ./internal/config`, `go test -race ./internal/config`, `go test ./...`, and `git diff --check 8105b5c^..b697220` successfully. It found one remaining High valid-configuration blocker: included values with an unquoted path, a quoted middle path, and a trailing unquoted path were over-wrapped by the stripped-quote restoration heuristic. Quote restoration now occurs only when the dependency-stripped first and last quote boundaries are both visibly embedded in non-whitespace text; already balanced first, middle, or last quoted fields are parsed as supplied. Regression coverage now includes quoted-middle ordering and included Windows drive/UNC values. The reviewer also noted existing configured-path disclosure in errors and same-user/privileged known-host replacement between `Stat` and open; these are explicitly retained because actionable configured-path errors and owner-controlled SSH trust files are existing assumptions, while no key, passphrase, token, or host-key material is exposed. `gofmt`, the focused Git SSH regression tests, `go test ./internal/config`, and `go test -race ./internal/config` passed on 2026-07-27. Renewed independent approval of this final follow-up remains required.
- `AZ-N-yxh3_y5QfimGTZ0` (Tier 0, task 7.4 approval): a fourth independent fresh-context builtin `reviewer` reviewed the complete Git SSH slice through `2e21572` and explicitly marked it **APPROVED** with no blockers. The approval checklist covered direct and included single/all-quoted/escaped/all-unquoted/mixed-first/middle/last paths, repeated user/global directives, Windows/UNC paths, ambiguity failure, order/legacy fields, command `GlobalKnownHostsFile` and both `-o` forms, precedence, aliases/ports/defaults, configured/default trust files, unknown/changed/hashed keys, configured-file failures, the warned explicit opt-out, fail-closed behavior, API stability, error disclosure, TOCTOU assumptions, and replacement complexity. The reviewer ran `go test ./internal/config`, `go test -race ./internal/config`, `go test ./...`, and `git diff --check 8105b5c^..2e21572`; all passed and the worktree was clean. The owning workflow then reran `go test ./internal/config`, `go test -race ./internal/config`, and `git diff --check`; all passed on 2026-07-27. All prior High review concerns are resolved; the configured-path disclosure and owner-controlled-file TOCTOU observations retain their documented dispositions.
- `AZ-N-y0t3_y5QfimGTZ7` (Tier 0, task 8.2): extracted six explicit phase helpers for protected policy, debug broker, debug builder, debug publisher, release, and Scorecard topology; `validateRepositoryTopology` invokes them in that unchanged order and delegates trusted Sonar validation last. The diff retains every predicate, compound short-circuit order, workflow/file/job lookup, release file loop and job iteration, action-prefix match, permission check, checkout/static-main/provenance predicate, and exact error string recorded by task 8.1. No workflow or generic policy-engine behavior changed. `gofmt`, `go test ./scripts/check-workflow-policy`, `go vet ./scripts/check-workflow-policy`, `bash scripts/check-workflow-security.sh`, `bash scripts/check-immutable-ci-dependencies.sh`, and `bash scripts/check-codeowners.sh` passed on 2026-07-27; `git diff --check` also passed. Local structural remediation is not a claim of Sonar closure.
- `AZ-N-y0t3_y5QfimGTZ7` (Tier 0, task 8.3 approval): an independent fresh-context builtin `reviewer` reviewed task 8.1 inventory commit `65d8418` and implementation commit `dc2d647`, compared the topology function line-by-line before and after extraction, and marked the slice **APPROVED** with no blockers. The checklist confirmed unchanged protected-policy, broker, builder, publisher, release, Scorecard, and trusted Sonar ordering; compound short-circuit order; exact errors; required-workflow failures; candidate trust boundary; permission checks; checkout/static-main restrictions; action-prefix semantics; independent immutable-pin validation; provenance; hostile mutation coverage; and no omitted, broadened, or candidate-controlled predicate. The reviewer ran `gofmt -d scripts/check-workflow-policy/main.go`, policy tests/vet, workflow-security, immutable-dependency, CODEOWNERS, and commit-range diff checks; all passed. The owning workflow reran formatting verification, `go test ./scripts/check-workflow-policy`, `go vet ./scripts/check-workflow-policy`, all three security scripts, and `git diff --check` on 2026-07-27; all passed. No review concerns remain.
- Replacement-smell task 7.7 approval: an independent fresh-context builtin `reviewer` reviewed `fe4d695` and `c60377b` against tasks 7.3–7.7 and the previously approved SSH slice, and marked both remediations **APPROVED** with no blockers. The reviewer confirmed unchanged host/node/include matching and order, direct/included parsing, ignored dependency lookup errors, ambiguity/error propagation, command precedence, defaults, strict-checking and fail-closed behavior; it also confirmed the complete test assertion inventory and that the decomposed source/test functions are structurally below cognitive complexity 15 without moving the smell. Formatting, focused tests, package/race tests, `go test ./...`, and commit-range diff checks passed. The owning workflow reran formatting verification, `go test ./internal/config`, `go test -race ./internal/config`, and `git diff --check` on 2026-07-27; all passed.
- `AZ-jUiMHZF5_tklsHNSX` (PR replacement, task 7.6): split the reported combined test into focused file-derived-value/legacy-first-entry, scalar-default, and command-precedence tests using one fixture decoder. Every scalar, identity, user/global ordered path, Windows/UNC, legacy first-field, and strict-host-key assertion remains unchanged; no production behavior changed. `gofmt`, all focused direct/included/platform/precedence tests, `go test ./internal/config`, `go test -race ./internal/config`, and `git diff --check` passed on 2026-07-27. Authenticated closure awaits the corrected PR analysis.
- `AZ-jUiJuZF5_tklsHNSW` (PR replacement, task 7.5): decomposed `sshConfigPathValues` into host, node, and include traversal helpers. Host matching, node order, direct and included path order, ignored dependency lookup errors, parse/ambiguity error propagation, and every reviewed trust behavior are unchanged; the reported orchestration no longer contains nested host/node/type/include traversal. `gofmt`, focused direct/included/platform path tests, `go test ./internal/config`, `go test -race ./internal/config`, and `git diff --check` passed on 2026-07-27. Authenticated closure awaits the corrected PR analysis.
- `AZ-N-y0t3_y5QfimGTZ2` (Tier 0): verified the three uses are the expected-permission map, the debug-publication job lookup, and its dependency check. Replaced only those literals with private `debugReleaseValidateJob`; YAML key, lookup behavior, predicate order, and error text are unchanged. `gofmt`, `go test ./scripts/check-workflow-policy`, `go vet ./scripts/check-workflow-policy`, and `bash scripts/check-workflow-security.sh` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ-N-y0t3_y5QfimGTZ4` (Tier 0): verified all seven uses are prefix arguments to `jobUses`/`stepWithContains`, while immutable SHA validation remains the separate `pinnedAction` predicate in `validateActions`. Replaced only these prefixes with private `githubScriptActionPrefix`; no action pin or trust predicate changed. `gofmt`, `go test ./scripts/check-workflow-policy`, `go vet ./scripts/check-workflow-policy`, and `bash scripts/check-workflow-security.sh` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZWKA6kX03lIpGBL3rEn` (Tier 2): verified the three uses are the flag declaration, Viper binding key, and persistent-flag lookup. Replaced only them with package-local `noSummaryFlag`; spelling, default, binding, and lookup semantics are unchanged. `gofmt` and `go test ./cmd/eirctl` passed on 2026-07-27.
- `AZZ8glSK1wqWcqTip8XF` (Tier 2): verified `WithReset` is required by `ExecutorIface`, `GetExecutorFactory` creates a new container executor for each execution, and `ContainerExecutor` has no resettable state. Added only an in-body explanatory comment. `gofmt` and `go test ./runner` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZxSHQDOhNFpWUf3zR1y` (Tier 2): verified `timeoutCancelFn` is created only for non-nil `job.Timeout`; `TestDefaultExecutor_Execute` exercises both a timed successful command and subsequent no-timeout execution. Moved the existing defer into the timeout branch, preserving cancellation timing for every timeout path. `gofmt` and `go test ./runner` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ-N-y1d3_y5QfimGTaC` and `AZ-N-y1d3_y5QfimGTaD` (Tier 1): replaced the two anonymous pull-request repository response types with the shared private `pullRepositoryResponse`, retaining `FullName`, its `json:"full_name"` tag, and the zero-value behavior. Base/head provenance, identity, path, blob, and head-recheck logic are unchanged. `gofmt`, `go test ./scripts/materialize-sonar-source`, `go vet ./scripts/materialize-sonar-source`, and `bash scripts/check-workflow-security.sh` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ-N-ywQ3_y5QfimGTZy` (Tier 0): inventoried and retained the success output/path, stream-copy error, and cache-directory creation error cases; the mock error paths now explicitly fail if an expected error is absent. Extracted environment/setup/execution assertions to `runStoreInCacheTest` without production-cache changes. `gofmt`, `go test ./internal/config -run Test_StoreInCache`, and `go test ./internal/config` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ-N-ywQ3_y5QfimGTZz` (Tier 0): retained successful YAML read, missing-file, unexpected-error, import success, and import-write-failure coverage. Extracted setup/execution/assertions to `runGetFromCacheTest` without production-cache changes and strengthened the existing test to fail on a missing successful-read context. `gofmt`, `go test ./internal/config -run Test_Get_fromCache`, and `go test ./internal/config` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZWKA6j703lIpGBL3rEk` (Tier 2): task 2.4 was corrected with user approval to reflect the current four occurrences of the `no-prompt` external key: two Viper lookups, the flag declaration, and the Viper binding/lookup. All four have the same external-name semantics. Replaced only those literals with private `noPromptFlag`; prompted/non-prompted behavior and Viper binding semantics are unchanged. `gofmt` and `go test ./cmd/eirctl` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ1t2WyVtvGp9fbwa0Kx` (Tier 2): split `cmdRunTestHelper` into context/command execution, error assertion, and output assertion helpers. The test helper still uses a supplied context (including the cancellation test) or `context.TODO`, constructs the same command, preserves error/output assertion behavior, and retains every caller. Helper call coverage remains 52 invocations in 11 test files before and after. `gofmt` and `go test ./cmd/eirctl` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ1t2Wy_tvGp9fbwa0K3` (Tier 2): table-driven `Test_main` retains the unknown-command, explicit debug, explicit verbose, failing-task, missing-task, and help cases. Named helpers restore `os.Args` and the global Logrus level with `t.Cleanup`, preventing subtest state leakage. Exit-code assertions remain `125`, `1`, and `0` respectively, and every case still requires non-empty output. `gofmt`, `go test ./cmd -run Test_main`, and `go test ./cmd` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ1t2WvrtvGp9fbwa0Km` (Tier 2): moved the prefixed-writer lifecycle and repeated containment assertions into named helpers. The existing header, transformed single-line and multi-line body, blank-line, trailing-newline, and footer expectations are unchanged; the expected body fragments retain their exact ANSI/CRLF byte sequences. `gofmt`, `go test ./output -run TestOutput_prefixedOutputDecorator`, and `go test ./output` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ1t2WzPtvGp9fbwa0K4` (Tier 2): replaced repeated merge/chaining map checks with `assertVariablesEqual`, which verifies exact cardinality plus every key/value and therefore rejects missing and extra entries. The overwrite, non-overwrite merge, chaining precedence, key-preservation, and value assertions are retained or strengthened; production `variables` code is unchanged. `gofmt`, `go test ./variables -run TestVariables_MergeV2`, `go test ./variables`, and `go test -race ./variables` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ1t2WjltvGp9fbwa0IF` (Tier 2): confirmed the nested pipeline fixture produces the expected generated workflow and retained every existing exact job/step ordering assertion. Extracted workflow generation/unmarshalling and repeated step-order assertions into named helpers; each expected job/index/name remains explicit, and missing steps now report diagnostic details. `gofmt`, `go test ./internal/genci -run TestGenCi_GithubImpl_ordering`, and `go test ./internal/genci` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ1t2WqptvGp9fbwa0Ir` (Tier 2): confirmed `TestMapKeys` used nested membership loops that accepted unexpected keys. Replaced them with cloned, sorted exact slices and `slices.Equal`, which verifies both missing and extra keys while retaining the non-map empty-key case. `gofmt`, `go test ./internal/utils -run TestMapKeys`, and `go test ./internal/utils` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ1t2W1PtvGp9fbwa0LE` (Tier 2): decomposed graph construction, successful node lookup, missing-node error, and root-child membership into focused subtests/helpers. All four expected successful nodes, `ErrNodeNotFound` for `stage7`, and each root child are asserted; root-child cardinality is now explicit. `gofmt`, `go test ./scheduler -run TestExecutionGraph_Nodes`, `go test ./scheduler`, and `go test -race ./scheduler` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZ1t2W0ytvGp9fbwa0K8` (Tier 2): compared `Test_Scheduler_Error_Required` and `Test_Scheduler_ConditionErroredStage` line-by-line: both construct the same two conditioned stages, use the same runner, schedule the same graph, and assert identical `Done`/`Skipped` statuses. No distinct required-input behavior is defined by the repository fixture, so removed only the misleading duplicate and retained the condition-error test. `gofmt`, `go test ./scheduler -run 'Test_Scheduler_(ConditionErroredStage|Error_Required)'`, `go test ./scheduler`, and `go test -race ./scheduler` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZbPupl0Qf_52EOIHgtb` (Tier 2): added a byte-exact characterization covering sorted completed, skipped, errored-with-trimmed-task-message, canceled, and unexpected statuses, including labels, ANSI colors, durations, and footer. Extracted sorting, per-status dispatch, and error rendering helpers without changing output text or stage-name mutation behavior. `gofmt`, `go test ./internal/cmdutils -run Test_PrintSummary`, and `go test ./internal/cmdutils` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZWKA6je03lIpGBL3rEh` (Tier 2): inspected compiler coverage for required values, rendering, variations, interactive tasks, and empty-job behavior. Extracted variable rendering and linked-list append helpers; command/variation order, template-error propagation, timeout, environment merging, and interactive mutation are unchanged. `gofmt`, `go test ./runner -run 'TestTaskCompiler_CompileTask'`, and `go test ./runner` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
- `AZWKA6je03lIpGBL3rEi` (Tier 2): confirmed `compileCommand` and all four callers are package-local. Replaced its ten positional parameters with private `compileCommandInput` and migrated task compilation, before, after, and condition callers without changing passed pointers, defaults, or ownership. `gofmt`, `go test ./runner -run 'TestTaskCompiler_CompileTask|TestTaskRunner'`, and `go test ./runner` passed on 2026-07-27. Local structural remediation is not a claim of Sonar closure.
