name: Lint and Test
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  set-version-tag:
    runs-on: ubuntu-24.04
    outputs:
      semVer: ${{ steps.gitversion.outputs.semVer }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@7417b1089e2c7de93510f1901d656ddf60bb024f # v4.7.0
        with:
          versionSpec: "6.2.0"
      - name: Set SemVer Version
        uses: gittools/actions/gitversion/execute@7417b1089e2c7de93510f1901d656ddf60bb024f # v4.7.0
        id: gitversion

  lint:
    name: Lint
    runs-on: ubuntu-24.04
    env:
      DOCKER_HOST: unix:///var/run/docker.sock
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5
      - name: Check maintained Go versions
        run: scripts/check-go-version.sh
      - name: Check immutable CI dependencies
        run: scripts/check-immutable-ci-dependencies.sh
      - name: Check CODEOWNERS coverage
        run: scripts/check-codeowners.sh
      - name: Advisory workflow policy feedback (not required)
        run: scripts/check-workflow-security.sh
      - name: Linters
        run: |
          go run -race cmd/main.go run lints

  test:
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-24.04
            docker_host: unix:///var/run/docker.sock
          # TODO: Fix Windows Test Pipeline, this will require our own
          # self-hosted agent...
          # - name: Windows
          #   os: windows-2025
          #   # docker_host: /var/run/docker.sock
    name: Test (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    needs:
      - set-version-tag
      - lint
    env:
      SEMVER: ${{ needs.set-version-tag.outputs.semVer }}
      DOCKER_HOST: ${{ matrix.docker_host }}
    steps:
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5

      - name: Check out code into the Go module directory
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Cache Go modules
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Check Schema
        # TODO: Schema Generation is broken on Windows atm
        if: matrix.name == 'Linux'
        run: |
          go run cmd/main.go generate_own_schema
        env:
          CI: true

      - name: Test
        run: |
          go run cmd/main.go run pipeline gha:unit:test --verbose

      - name: Upload inert JUnit test report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: junit-report-${{ matrix.name }}
          path: .coverage/report-junit.xml
          if-no-files-found: warn
          retention-days: 7

      - name: Upload bounded SonarCloud reports
        if: ${{ always() && matrix.name == 'Linux' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sonar-reports-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .coverage/out
            .coverage/report-junit.xml
          if-no-files-found: warn
          retention-days: 7

  sonarcloud:
    name: SonarCloud analysis
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Check out trusted main source with history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.5

      - name: Generate SonarCloud reports
        run: go run cmd/main.go run pipeline gha:unit:test --verbose

      - name: Scan trusted main source with SonarCloud
        uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f # v8.2.1
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          scannerVersion: 8.1.0.6389
          scannerBinariesUrl: https://binaries.sonarsource.com/Distribution/sonar-scanner-cli
          skipSignatureVerification: "false"
          args: >-
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.organization=ensono
            -Dsonar.projectKey=Ensono_eirctl
            -Dsonar.scm.revision=${{ github.sha }}
            -Dsonar.qualitygate.wait=true

  report:
    name: Publish test report
    if: always()
    needs: test
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      checks: write
    steps:
      - name: Download inert JUnit test report
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        continue-on-error: true
        with:
          pattern: junit-report-*
          path: .coverage
          merge-multiple: true

      - name: Publish JUnit style test report
        uses: mikepenz/action-junit-report@3585e9575db828022551b4231f165eb59a0e74e3 # v5
        if: ${{ always() && hashFiles('.coverage/report-junit.xml') != '' }}
        with:
          report_paths: ".coverage/report-junit.xml"
          commit: ${{ github.sha }}
          fail_on_failure: true
          check_name: Eirctl Unit Tests
